# Headless Claude Code auth — run `claude setup-token` and paste the result here.
CLAUDE_CODE_OAUTH_TOKEN=

# From @BotFather in Telegram after /newbot
TELEGRAM_BOT_TOKEN=

# Optional, only used by src/server.py (the n8n path)
PORT=8000
